// Auth commands are defined in auth/commands.rs with #[tauri::command] attributes.
// The functions are re-exported from there and used directly in the invoke handler.
// No additional re-exports needed here.
